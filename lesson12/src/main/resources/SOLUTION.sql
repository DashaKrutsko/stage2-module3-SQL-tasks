SET REFERENTIAL_INTEGRITY FALSE; BEGIN TRANSACTION; DELETE FROM STUDENT WHERE  ID IN(SELECT MARK.STUDENT_ID FROM MARK WHERE MARK.SUBJECT_ID IN (SELECT SUBJECT.ID FROM SUBJECT WHERE SUBJECT.GRADE>=4));DELETE FROM MARK WHERE MARK.SUBJECT_ID IN (SELECT SUBJECT.ID FROM SUBJECT WHERE SUBJECT.GRADE>=4);DELETE FROM SUBJECT WHERE SUBJECT.GRADE>=4;COMMIT;SET REFERENTIAL_INTEGRITY TRUE;
